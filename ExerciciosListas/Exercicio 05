##5.Faça um Programa que leia 20 números inteiros e 
# armazene-os num vetor. Armazene os números pares no vetor 
# PAR e os números IMPARES no vetor impar. Imprima os três vetores.

r = 0

num = []
num_par = []
num_impar = []

while r < 20: 
    try:
        numero = int(input ('Digite um número: '))
        num.append(numero)
        r = r + 1 
        
        if numero % 2 == 0:
            num_par.append(numero)
        else:
            num_impar.append(numero)
        
    except:
          print('Digite um número inteiro')
print (num)
print (num_par)
print (num_impar)
